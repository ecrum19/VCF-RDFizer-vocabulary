#!/usr/bin/env bash
set -euo pipefail

repo_root=$(git rev-parse --show-toplevel)
"$repo_root/scripts/sync-docs-assets.sh"

# Ensure generated assets are included in the commit.
git add docs/assets docs/.nojekyll

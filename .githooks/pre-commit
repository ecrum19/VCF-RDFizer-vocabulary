#!/usr/bin/env bash
set -euo pipefail

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

# The companion site is generated in ignored site/ output and deployed by
# GitHub Actions. Validate its source configuration without staging artifacts.
npm run ocg:check
